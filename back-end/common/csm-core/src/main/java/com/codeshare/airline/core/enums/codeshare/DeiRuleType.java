package com.codeshare.airline.core.enums.codeshare;

public enum DeiRuleType {
    VALIDATE_ONLY,   // must exist
    INJECT,          // inject if missing
    OVERRIDE         // overwrite existing
}