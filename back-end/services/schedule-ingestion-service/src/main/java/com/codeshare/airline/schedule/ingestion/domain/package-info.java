/**
 * Core ingestion domain model.
 *
 * <p>Contains workflow context objects, enums, and metadata used across extraction,
 * parsing, validation, and persistence. It should not depend on web or database code.
 */
package com.codeshare.airline.schedule.ingestion.domain;
